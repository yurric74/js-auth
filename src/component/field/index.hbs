<div class='field'>
  <label
    for='{{name}}'
    class='field__label'
  >{{label}}</label>
  <input
    oninput='{{action}}(this.name, this.value)'
    type='{{type}}'
    class='field__input validation'
    name='{{name}}'
    placeholder='{{placeholder}}'
  />
</div>